# ============================================================================
# EDGE-RAG UNIFIED CONFIGURATION
# ============================================================================
#
# Version: 4.0.0
# Author: Jan Nietzard
# Last Modified: 2026-04-24
#
# Single source of truth for all runtime parameters.
# All Python modules read this file via src/logic_layer/_settings.py
# or via yaml.safe_load() — no values are hardcoded in production code.
#
# For thesis evaluation: use requirements_frozen.txt and this file unchanged.
# ============================================================================

# ============================================================================
# EMBEDDING MODEL
# ============================================================================
embeddings:
  # Model name (must match an Ollama installed model)
  # Alternative (better score distribution, requires re-ingestion):
  #   model_name: "BAAI/bge-small-en-v1.5"   # via sentence-transformers
  # Changing this requires re-running ingestion (stored vectors become stale).
  model_name: "nomic-embed-text"

  # Ollama API endpoint
  base_url: "http://localhost:11434"

  # Embedding dimensionality
  # nomic-embed-text: 768
  # all-MiniLM-L6-v2: 384
  # bge-base-en: 768
  embedding_dim: 768

  # SQLite cache for embedding results (persists across runs)
  cache_path: "./cache/embeddings.db"

# ============================================================================
# DOCUMENT CHUNKING
# ============================================================================
chunking:
  # Mode: "standard" (fixed size) or "semantic" (structure-aware)
  mode: "standard"

  # Standard chunking parameters
  chunk_size: 512         # Characters per chunk
  chunk_overlap: 128      # Overlap between chunks

  # Semantic chunking parameters (used when mode="semantic")
  semantic:
    min_chunk_size: 200
    max_chunk_size: 1000
    detect_structure: true
    quality_filter: true


# ============================================================================
# VECTOR STORE (LanceDB)
# ============================================================================
vector_store:
  provider: "lancedb"
  db_path: "./data/vector"

  # =========================================================================
  # DISTANCE METRIC — CRITICAL SETTING
  # =========================================================================
  # Must be "cosine" for text embeddings.
  # LanceDB defaults to L2 which produces incorrect similarity scores.
  distance_metric: "cosine"

  # L2 normalisation for numerical stability
  normalize_embeddings: true

  # Retrieval parameters
  # top_k_vectors raised from 10 -> 20 (audit 2026-05-06):
  # under nomic-embed-text score compression, bridge-answer chunks routinely
  # rank 11-15. BM25 + cross-encoder reranker downstream make the wider funnel
  # cheap. similarity_threshold remains at 0.3 because raising it filters
  # nothing under compressed scores (all > 0.7) and lowering hurts no one.
  top_k_vectors: 20
  similarity_threshold: 0.3    # Minimum similarity [0.0, 1.0]
  overfetch_factor: 3          # ANN candidates = top_k * overfetch_factor
  graph_text_max_chars: 500    # Max characters stored in graph node

# ============================================================================
# KNOWLEDGE GRAPH
# ============================================================================
graph:
  enabled: true

  # =========================================================================
  # BACKEND SELECTION
  # =========================================================================
  # Only "kuzu" is supported in v4.0.0.
  # NetworkX fallback was removed — KuzuDB is required.
  # Install: pip install kuzu
  # =========================================================================
  backend: "kuzu"

  # Database path (KuzuDB: directory)
  graph_path: "./data/graph"

  # Graph traversal settings
  # top_k_entities raised from 5 -> 10 (audit 2026-05-06): mirrors widening of
  # vector top-k. KuzuDB queries are <30 ms, so a wider funnel is essentially
  # free and improves bridge-entity recall.
  max_hops: 2                    # Maximum path length for traversal
  top_k_entities: 10             # Number of graph results
  expand_context: true           # Include surrounding chunks (KuzuDB only)

  # Entity extraction method during ingestion
  # "keyword" - Simple keyword-based (fast, basic)
  # "spacy"   - spaCy NER (requires: pip install spacy)
  # "gliner"  - GLiNER zero-shot NER (requires: pip install gliner)
  entity_extraction_method: "keyword"

  # Relationship types stored in graph
  relation_types:
    - "from_source"     # Chunk → Document
    - "next_chunk"      # Chunk → Chunk (sequential)
    - "mentions"        # Chunk → Entity
    - "related_to"      # Entity → Entity

# ============================================================================
# RETRIEVAL AUGMENTED GENERATION (RAG)
# ============================================================================
rag:
  # =========================================================================
  # RETRIEVAL MODE
  # =========================================================================
  # Options:
  #   "vector" - Vector similarity only (baseline)
  #   "graph"  - Graph traversal only (structural)
  #   "hybrid" - Weighted combination (recommended)
  # =========================================================================
  retrieval_mode: "hybrid"

  # =========================================================================
  # ABLATION MODE
  # =========================================================================
  # The current Hybrid path is RRF-only (see rrf_k below). Weighted-fusion
  # ablation can be performed by setting retrieval_mode to "vector" or "graph"
  # to disable the other paths entirely. The previous vector_weight /
  # graph_weight knobs were never read by production code and were removed
  # in the 2026-05-06 cleanup audit.
  # =========================================================================

  # RRF Fusion Parameter
  # Note: rrf_k here is consumed by HybridRetriever (data layer) for its own
  # internal fusion pass. The Navigator (logic layer) reads navigator.rrf_k
  # separately for its RRF fusion over sub-query result lists. Both default
  # to 60 (Cormack et al., 2009) but serve different components and must be
  # maintained independently.
  rrf_k: 60                    # Reciprocal Rank Fusion constant (standard: 60)
  cross_source_boost: 1.2      # Boost for chunks appearing in both retrieval paths

  # BM25 sparse retrieval (§12.29 — Tier 1 improvement)
  # Third retrieval path alongside dense vector search and graph traversal.
  # No model download required; index built lazily from LanceDB on first query.
  # Requires: pip install rank_bm25
  # bm25_top_k raised from 10 -> 20 (audit 2026-05-06): BM25 cost is O(N) but
  # tiny; widening funnel improves bridge-chunk recall before fusion.
  enable_bm25: true             # Set false to disable for ablation studies
  bm25_top_k: 20                # BM25 candidates per query

# ============================================================================
# LLM CONFIGURATION (Artifact B — Logic Layer)
# ============================================================================
llm:
  # Active model — must be pulled via: ollama pull <model_name>
  model_name: "llama3.2:3b"   # llama3.2:3b: ~35-55s per query on CPU (safest for 60s timeout)

  base_url: "http://localhost:11434"
  temperature: 0.0    # 0.0 = fully deterministic — required for reproducible thesis evaluation
  max_tokens: 200
  timeout: 60         # Ollama API timeout in seconds
                      # Comparison model latency on CPU (ablation study):
                      # qwen2.5:3b ~10-20s, gemma2:2b ~20-35s,
                      # llama3.2:3b ~35-55s, phi3 ~45-80s (ablation model, borderline at 60s).

  # Context budget for Verifier (S_V)
  #
  # After Navigator filtering, at most max_docs chunks are forwarded to S_V.
  # Each chunk is truncated to max_chars_per_doc characters.
  # The assembled context is then capped at max_context_chars as a safety net.
  #
  # Theoretical max: max_docs(5) × max_chars_per_doc(500) = 2500 chars
  # Safety cap:      max_context_chars = 2500 chars (~625 tokens input to LLM)
  # Prompt overhead: ~150 chars → ~2650 chars total input
  #
  # Context budget evolution:
  #   900 -> 2500 (2026-05-04, §12.24): bridge chunks were truncated.
  #   2500 -> 3500, max_chars_per_doc 500 -> 800 (audit 2026-05-06):
  #   IFFHS-style answer sentences plus minimal bio context routinely span
  #   600-700 chars; 500 truncated the answer. qwen2.5:3b has a 4096-token
  #   context window (~12 kB); the previous 2500-char cap used only ~20%.
  #   3500 chars uses ~30% and leaves headroom for the prompt template.
  # max_docs stays at 5 to preserve context-window discipline.
  max_context_chars: 3500   # Safety cap (overrides theoretical max)
  max_docs: 5               # Max chunks forwarded to Verifier after Navigator filtering
  max_chars_per_doc: 800    # ~200 words per chunk

# ============================================================================
# NAVIGATOR (S_N) — Pre-Generative Filtering
# ============================================================================
navigator:
  # Relevance Filter: keep chunks with score >= factor × max_rrf_score
  # Lowered from 0.85 -> 0.6 (audit 2026-05-06): under nomic-embed-text score
  # compression, the 0.85 cutoff routinely evicted bridge chunks that scored
  # marginally below the top result. 0.6 still removes scoring outliers while
  # preserving the answer-bearing chunk in multi-hop traces.
  relevance_threshold_factor: 0.6

  # Redundancy Filter: Jaccard similarity threshold for deduplication
  redundancy_threshold: 0.8

  # Maximum chunk count after filtering (before context shrinkage)
  # Kept at 8 (audit 2026-05-06): prevents the double-trim pathology where the
  # Navigator caps at N, then the Verifier drops to max_docs=5 with a different
  # ranking criterion. 8 leaves a 3-chunk margin for the Verifier reorder
  # (§12.27). NOTE: bridge chunks that the cross-encoder would otherwise demote
  # past rank 8 are handled at the source — §12.30 makes the reranker score each
  # chunk against the sub-query that retrieved it (not the surface query), and
  # §12.31 (Planner Pattern H) decomposes chained-attribution bridges so the
  # bridge chunk is retrieved by its own focused hop.
  max_context_chunks: 8

  # Reciprocal Rank Fusion smoothing constant (standard: 60)
  # Reference: Cormack et al. (2009). SIGIR. DOI: 10.1145/1571941.1572114
  rrf_k: 60

  # Top-K results per sub-query from HybridRetriever
  # Raised from 10 -> 20 (audit 2026-05-06): matches upstream widening; the
  # gain from vector_top_k=20 and bm25_top_k=20 is wasted if the Navigator
  # only consumes 10 results per sub-query.
  top_k_per_subquery: 20

  # Cross-source corroboration boost weights for RRF fusion.
  # A chunk appearing in N sources/M sub-queries receives:
  #   boost = 1 + source_weight*(N-1) + query_weight*(M-1)
  # Empirically tuned on HotpotQA dev set (thesis section 3.3).
  corroboration_source_weight: 0.1
  corroboration_query_weight: 0.05

  # Cross-encoder reranker (§12.29 — Tier 1 improvement)
  # Applies after RRF fusion; re-ranks top-K by (query, chunk) relevance score.
  # Requires: pip install sentence-transformers  (+22 MB model on first use)
  # Enabled by default after the 2026-05-06 audit: the 22 MB cross-encoder is
  # edge-feasible (~30 ms/pair on CPU). Toggle false only for strict no-model-
  # download deployments or for ablation comparison runs.
  enable_reranker: true
  reranker_model: "cross-encoder/ms-marco-MiniLM-L-6-v2"
  reranker_top_k: 10

  # Contradiction filter thresholds (numeric heuristic).
  # Disabled by default (§12.33): the heuristic fires on population figures vs.
  # years within topically related chunks, repeatedly deleting the answer chunk.
  # The Verifier's NLI-based contradiction detection is the real guard.
  # Enable only for ablation comparisons.
  enable_contradiction_filter: true
  contradiction_overlap_threshold: 0.3
  contradiction_ratio_threshold: 2.0
  contradiction_min_value: 100.0

# ============================================================================
# PLANNER (S_P) — Query Analysis and Plan Generation
# ============================================================================
planner:
  # Minimum NER confidence for entity extraction.
  min_entity_confidence: 0.7

  # Maximum number of entities to extract per query
  max_entities: 10

  # Enable bridge entity detection (for multi-hop queries)
  enable_bridge_detection: true

  # Parse temporal constraints from queries
  enable_temporal_parsing: true

  # ---------------------------------------------------------------------------
  # CLASSIFIER WEIGHT CONSTANTS
  # Empirically tuned on HotpotQA dev set (thesis Section 4.3 ablation).
  # All six values can be varied here without code changes.
  # ---------------------------------------------------------------------------
  # Score bonus for SpaCy Matcher syntactic hits (Phase 2 of classify())
  classifier_spacy_weight: 1.5
  # Score bonus when entity/noun density indicates multi-hop (Phase 3)
  classifier_entity_boost: 0.5
  # Base term in confidence formula: conf = min(cap, base + score * scale)
  classifier_confidence_base: 0.6
  # Score multiplier in confidence formula
  classifier_confidence_scale: 0.15
  # Upper cap on returned classification confidence
  classifier_confidence_cap: 0.95
  # Confidence returned when no pattern matches (SINGLE_HOP fallback)
  classifier_fallback_confidence: 0.8

  # ---------------------------------------------------------------------------
  # ENTITY DENSITY THRESHOLDS (Phase 3 of classify())
  # Queries with more than these counts receive a multi-hop score boost.
  # ---------------------------------------------------------------------------
  entity_density_threshold: 2    # Named entity count threshold
  noun_density_threshold: 4      # Noun/proper-noun count threshold

  # Confidence assigned to entities found via regex fallback (not SpaCy NER)
  regex_entity_confidence: 0.75

# ============================================================================
# AVAILABLE MODELS (for multi-model evaluation and ablation)
# All models must be installed via: ollama pull <name>
# ============================================================================
available_models:
  # Thesis comparison models — accuracy vs. latency on edge CPU (1.5B–3.8B)

  - name: "qwen2:1.5b"
    description: "Qwen 2 1.5B — fastest model, reliably under 60s"
    params_b: 1.5
    quantization: "Q4_K_M"
    expected_latency_s: 10
    safe_60s: true

  - name: "gemma2:2b"
    description: "Gemma 2 2B — good quality/speed ratio (Google)"
    params_b: 2.0
    quantization: "Q4_K_M"
    expected_latency_s: 25
    safe_60s: true

  - name: "llama3.2:3b"
    description: "Llama 3.2 3B — Meta reference for the 3B class"
    params_b: 3.0
    quantization: "Q4_K_M"
    expected_latency_s: 45
    safe_60s: true

  - name: "phi3"
    description: "Phi-3 Mini 3.8B — Microsoft edge flagship, borderline at 60s"
    params_b: 3.8
    quantization: "Q4_K_M"
    expected_latency_s: 55
    safe_60s: false

  - name: "qwen3:4b"
    description: "Qwen 3 4B — improved multi-hop reasoning, ~3.5 GB RAM"
    params_b: 4.0
    quantization: "Q4_K_M"
    expected_latency_s: 35
    safe_60s: true

# ============================================================================
# AGENTIC CONTROLLER (Artifact B)
# ============================================================================
agent:
  # Maximum self-correction iterations in the Verifier loop.
  # Lowered from 2 -> 1 (audit 2026-05-06): Madaan et al. (2023) Self-Refine
  # results reproduce on GPT-3.5+; on a 1.5B-parameter SLM the second pass
  # typically injects hallucinations rather than correcting them. Set to 2
  # only as an ablation comparison.
  max_verification_iterations: 1

  # Enable/disable individual pipeline stages (ablation flags)
  enable_planner: true
  enable_verifier: true

  # FIFO query-result cache (SHA-256-keyed, insertion-order eviction)
  enable_caching: true
  cache_max_size: 1000

# ============================================================================
# QUANTIZATION SETTINGS
# ============================================================================
quantization:
  enabled: true
  bits: 4           # 4-bit quantization (llama.cpp default)
  group_size: 128   # Group size for quantization

# ============================================================================
# DATA PATHS
# ============================================================================
paths:
  root: "./"
  data: "./data"
  documents: "./data/documents"
  vector_db: "./data/vector"
  graph_db: "./data/graph"
  logs: "./logs"
  cache: "./cache"

# ============================================================================
# LOGGING
# ============================================================================
logging:
  level: "INFO"      # DEBUG, INFO, WARNING, ERROR
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file: "./logs/edge_rag.log"

# ============================================================================
# PERFORMANCE TUNING
# ============================================================================
performance:
  # Embedding batch size (higher = faster ingestion, more memory)
  batch_size: 64

  # Number of worker threads for parallel processing
  num_workers: 2

  # Compute device: "cpu" or "cuda" (if available)
  device: "cpu"

  # Enable embedding cache (SQLite-based)
  cache_embeddings: true

  # Maximum cache size in MB
  max_cache_size_mb: 512

# ============================================================================
# BENCHMARK CONFIGURATION (for thesis evaluation)
# ============================================================================
benchmark:
  # Available datasets for evaluation
  datasets:
    - "hotpotqa"
    - "2wikimultihop"
    - "strategyqa"

  # Default number of samples per dataset
  default_samples: 500

  # Ablation study configurations
  ablation_configs:
    - name: "vector_only"
      vector_weight: 1.0
      graph_weight: 0.0
    - name: "graph_only"
      vector_weight: 0.0
      graph_weight: 1.0
    - name: "hybrid_70_30"
      vector_weight: 0.7
      graph_weight: 0.3
    - name: "hybrid_50_50"
      vector_weight: 0.5
      graph_weight: 0.5

# ============================================================================
# VERIFIER (S_V) — Pre-Validation and Self-Correction
# ============================================================================
verifier:
  # ---------------------------------------------------------------------------
  # PRE-GENERATION VALIDATION FLAGS
  # The Verifier-side NLI contradiction detection (270 MB cross-encoder
  # download) was removed in the 2026-05-06 cleanup audit because it had been
  # disabled by default since v3.x and conflicted with the edge-deployment
  # constraint of the thesis. Reactivate by adding the relevant fields back.
  # ---------------------------------------------------------------------------
  enable_entity_path_validation: true
  enable_credibility_scoring: true

  # Entity-path validation: minimum fraction of query entities that must
  # appear in the retrieved context for the check to pass.
  # Lowered from 0.5 -> 0.34 (audit 2026-05-06): for 3-entity bridge queries
  # (subject + bridge + object), 0.5 demanded 2/3 entities present, but the
  # bridge entity is unknown at S_P time and is added only after Hop 0. A
  # 1/3 floor lets the gold chunk pass on the first hop.
  entity_coverage_threshold: 0.34

  # Source credibility scoring thresholds.
  min_credibility_score: 0.5       # Chunks below this are filtered

  # Credibility signal weights — must sum to 1.0.
  # Empirically chosen on a HotpotQA dev-set sample; see thesis Section 3.4.
  credibility_weight_cross_ref: 0.4     # Cross-reference corroboration
  credibility_weight_entity_freq: 0.3   # Named-entity density
  credibility_weight_provenance: 0.3    # Graph vs. vector retrieval origin

  # Credibility scoring normalisation constants.
  credibility_cross_ref_max: 3.0        # Divisor for cross-reference count
  credibility_provenance_baseline: 0.5  # Score for vector-only (non-graph) chunks

  # SpaCy processing limit inside credibility scoring (edge CPU budget).
  spacy_max_chars: 500

  # ConfidenceLevel thresholds for VerificationResult.confidence property.
  # A verified-claim ratio >= high_threshold → HIGH, >= medium_threshold → MEDIUM.
  confidence_high_threshold: 0.8
  confidence_medium_threshold: 0.5

  # Claim extraction and entity verification limits.
  min_claim_chars: 15       # Minimum characters for a sentence to count as a claim
  max_entities_to_verify: 5 # Max entities checked per claim during verification
  max_key_phrases: 10       # Max key phrases per chunk in cross-reference scoring

# ============================================================================
# INGESTION CONFIGURATION
# ============================================================================
ingestion:
  # Available strategies: sentence | sentence_spacy | semantic | fixed | recursive
  chunking_strategy: "sentence_spacy"

  # Sentence strategy (thesis default: 3 sentences per chunk, 1 overlap)
  # Alternative (finer granularity, requires re-ingestion):
  #   sentences_per_chunk: 2   # IFFHS award sentence → own chunk → perfect BM25 hit
  sentences_per_chunk: 3
  sentence_overlap: 1

  # Fixed/Recursive/Semantic strategy
  chunk_size: 1024
  chunk_overlap: 128
  min_chunk_size: 50

  # Semantic strategy
  min_lexical_diversity: 0.3
  min_information_density: 2.0

  # Fixed chunking: word-boundary heuristic
  # Break at last space if it falls within the last (1 - factor) * chunk_size chars.
  # 0.8 = break at word boundary when space is within the last 20% of the chunk.
  word_boundary_factor: 0.8

  # Maximum character length per sentence-based chunk (SpacySentenceChunker)
  max_chunk_chars: 2000

  # SpaCy-specific settings
  spacy_model: "en_core_web_sm"
  entity_aware_chunking: false

  # Quality filter thresholds (AutomaticQualityFilter — semantic chunking only)
  quality_filter:
    min_length: 100   # Minimum characters per chunk
    min_words: 15     # Minimum word count per chunk

  # Metadata and entity extraction during ingestion
  extract_entities: true
  add_source_metadata: true

# ============================================================================
# ENTITY EXTRACTION (GLiNER + REBEL)
# ============================================================================
entity_extraction:

  # GLiNER Named Entity Recognition
  # Reference: Zaratiana et al. (2023). arXiv:2311.08526.
  gliner:
    model_name: "urchade/gliner_small-v2.1"
    # ─────────────────────────────────────────────────────────────────────
    # FIXED: OntoNotes-5 core entity-type set
    # Reference: Weischedel, R. et al. (2013). OntoNotes Release 5.0.
    #            Linguistic Data Consortium, LDC2013T19.
    #
    # 9 GLiNER prompts → 8 canonical types (via GLINER_LABEL_MAP):
    #   person                       → PERSON
    #   organization                 → ORGANIZATION
    #   location                     → LOCATION
    #   city, country                → GPE
    #   date                         → DATE
    #   event                        → EVENT
    #   work of art                  → WORK_OF_ART
    #   product                      → PRODUCT
    #
    # The multi-prompt expansion (city + country alongside location) gives
    # GLiNER higher recall than a single abstract label — Zaratiana et al.
    # (2023, §4.2). All prompts collapse to OntoNotes-5 canonical types in
    # the graph, so retrieval semantics are unchanged.
    #
    # ⚠ DO NOT add domain-specific types (state, landmark, album, film,
    # movie, award, ...). They transfer poorly across the three benchmark
    # datasets (HotpotQA + 2WikiMultiHopQA + StrategyQA) and break the
    # scientific reproducibility of the thesis evaluation. This list is
    # fixed by deliberate design — same set used by spaCy en_core_web_sm
    # and by Stanford NER (Manning et al. 2014).
    # ─────────────────────────────────────────────────────────────────────
    entity_types:
      - "person"
      - "organization"
      - "location"
      - "city"
      - "country"
      - "date"
      - "event"
      - "work of art"
      - "product"
    confidence_threshold: 0.15   # recall-optimised; junk filtered downstream
    batch_size: 16

  # REBEL Relation Extraction
  # Reference: Cabot & Navigli (2021). EMNLP 2021 Findings.
  rebel:
    model_name: "Babelscape/rebel-large"
    confidence_threshold: 0.5    # uniform sentinel — REBEL emits no per-triplet score
    batch_size: 8                # reserved for future batched implementation
    min_entities_for_re: 2       # skip RE when fewer entities are found (~60% reduction)
    max_input_length: 256        # tokenizer truncation length
    max_output_length: 256       # generation length cap
    num_beams: 5                 # beam search width

  # Entity caching (in-memory LRU + SQLite persistent store)
  caching:
    enabled: true
    cache_path: "./data/entity_cache.db"
    lru_cache_size: 10000
